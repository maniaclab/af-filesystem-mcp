apiVersion: v1
kind: Service
metadata:
  name: {{ include "af-filesystem-mcp.fullname" . }}
  labels:
    {{- include "af-filesystem-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "af-filesystem-mcp.selectorLabels" . | nindent 4 }}
